# 学校説明会案内サイト 仕様書

# 1. システム概要

学校説明会の来場者向けに、校内マップ・教室検索・スタンプ機能を提供するWebサイトを開発する。

利用者はスマートフォンのブラウザからアクセスし、アプリのインストール不要で利用できる。

---

# 2. 開発目的

- 初めて学校を訪れる来場者が目的の教室を探しやすくする
- 各教室で実施している説明会・体験内容を分かりやすく表示する

---

# 3. 開発環境

| 項目 | 内容 |
|------|------|
| フロントエンド | React (Vite) |
| デプロイ | Vercel |
| データ管理 | JSONファイル |
| API | 使用しない |

---

# 4. システム構成

## システム構成

- 校内マップ
- 教室検索
- スタンプ機能

## データ構成

教室情報は `src/data/campus_map_data.json` で管理する。

1つのJSONファイルにすべての教室情報を格納し、`floor`プロパティでフロアを管理する。

フロアは `floor_1F` ～ `floor_5F` を使用する。

```json
[
  {
    "id": "N003",
    "floor": "floor_4F",
    "x": 49,
    "y": 396,
    "name": "S402",
    "type": "room"
  },
  {
    "id": "N004",
    "floor": "floor_4F",
    "x": 162,
    "y": 396,
    "name": "S404",
    "type": "room"
  }
]

```

※ `name` は部屋番号を表す。
※　座標は `src/map` 内の画像の左上を原点(0,0)として、右方向をX軸、下方向をY軸とする。

| type | 内容 |
|------|------|
| room | 通常教室 |
| stamp | スタンプ対象教室 |
| branch | 分岐点(廊下など) | 
| entrance | 入口 |
| stairs | 階段/EV |


stampはスタンプラリー対象の教室を表す。


# 5. 機能一覧

## 5.1 校内マップ

### 概要

校内マップ画像を表示し、教室・廊下・階段などをマップ上に表示する。
type が `room` または `stamp` の地点をクリック可能とする。

検索(5.2記載)後は対象教室を強調表示する。

### 構成

- 各フロア画像を中央に表示する
- フロア切替はページ上部のラジオボタンで行う
- type が `room` または `stamp` の地点をクリック可能とする
- `react-zoom-pan-pinch` を利用し、マップの拡大・縮小・移動を可能にする

### 画面構成

#### 上部

- フロア切替（1〜5階）
- ラジオボタン

#### 下部

- 校内マップ

### クリック

教室をクリックすると以下を表示する。

- 部屋番号
- フロア

### マップ画像

- `src/map` 内の画像を使用する
- `src/data/campus_map_data.json` の座標に円を表示

|type	|色
|------|------|
|room	|黄色
|stamp	|青色
|branch	|灰色
|entrance	|緑色
|stairs	|水色

- 座標は `src/data/campus_map_data.json` の `x`、`y` 座標を使用する

### 強調表示

検索された教室の座標の円に赤い枠線を表示する。

新しい教室を選択した場合は、以前の強調表示を解除する。

---

## 5.2 教室検索

### 概要

部屋番号から教室を検索する。

### 検索仕様

- 部屋番号は部分一致検索とする

例

```
入力：S4
```

検索候補

```
S401
S402
S403
```

候補をタップすると、該当フロアへ切り替え、対象教室をマップ上で強調表示する。

### 機能

1. 部屋番号を入力する
2. 入力内容に一致する候補を表示する
3. 候補を選択する
4. 該当フロアへ自動で切り替える
5. 対象教室をマップ上で強調表示する

### エラー処理

検索結果が存在しない場合は、以下のメッセージを表示する。

```
該当する教室が見つかりません
```

---

## 5.3 スタンプ機能

### 概要

展示場所に設置されたQRコードを読み取ることでスタンプを取得する。

### QRコード仕様

QRコードURLの末尾に部屋番号を付与する。

例

```
https://ask-place.vercel.app/stamp/S305
```

### 動作

QRコードを読み取ると、以下の処理を行う。

1. URLから部屋番号を取得する
2. JSONからフロア情報を取得する
3. 該当フロアへ自動で切り替える
4. マップ上で該当教室を強調表示する
5. スタンプを取得する

### スタンプ仕様

- スタンプ取得状況はLocalStorageに保存する
- 同じQRコードを読み取っても重複取得しない
- 全スタンプ取得後は以下を表示する

```
すべてのスタンプを取得しました
```

### 保存仕様

保存キー

```
stampData
```

保存例

```json
[
  "S101",
  "S305",
  "N301"
]
```

### スタンプ対象教室

`src/data/campus_map_data.json` の `type` が `"stamp"` の教室をスタンプ対象とする。

---

# 6. ディレクトリ構成

```
src
├── assets
│   └── imgs
├── components
├── data
│   └── campus_map_data.json
├── map
│   ├── F1.png
│   ├── F2.png
│   ├── F3.png
│   ├── F4.png
│   └── F5.png
└── pages
```

---

# 7. 画面

## 配置

- Header
- トップ画像（`src/assets/imgs`）
- 部屋番号検索フォーム
- 校内マップ
- Footer

---

## 教室情報表示

表示内容

- 部屋番号
- フロア

---

# 8. 非機能要件

- スマートフォン対応
- レスポンシブ対応
- APIを使用しない
- JavaScriptが有効なブラウザで動作すること
- Vercelで公開可能であること
- LocalStorageを利用してスタンプ情報を保持する